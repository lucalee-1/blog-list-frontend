import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#93B5C6',
    },
    secondary: {
      main: '#FFE3E3',
    },
    intermediary: {
      main: 'C9CCD5',
    },
  },
  shape: {
    borderRadius: 8,
  },
});
