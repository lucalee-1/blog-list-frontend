import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#6E85B7',
    },
    secondary: {
      main: '#F2D1D1',
    },
    intermediary: {
      main: 'D3CEDF',
    },
  },
  shape: {
    borderRadius: 8,
  },
});
